# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (maxfiy kalitlar — git'ga kirmasin!)
.env
.env.local
.env.*.local

# Android build artifacts (source fayllari saqlanadi, faqat build o'chiriladi)
android/.gradle/
android/app/build/
android/build/
android/local.properties

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build artifacts va mahalliy sinov fayllari
*.apk
.env.development
server/sample_orders.sql
server/add_test_users.mjs

# Firebase maxfiy fayllar (git ga kirmasin!)
android/app/google-services.json
server/firebase-admin.json

# Lokal git worktree'lar (subagent-driven development uchun)
.worktrees/
